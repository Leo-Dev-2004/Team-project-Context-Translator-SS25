{
  "private": true,
  "name": "context-translator-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd packages/electron && npm run dev",
    "build": "cd packages/electron && npm run build",
    "clean": "cd packages/electron && npm run clean"
  }
}